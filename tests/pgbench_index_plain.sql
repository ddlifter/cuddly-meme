-- ============================================================================
-- pgbench Index Scan Test
-- ============================================================================
-- Поиск по PK-индексу (обычная таблица)
\set idx random(1, 1000)
SELECT id, balance FROM t_plain WHERE id = :idx;
