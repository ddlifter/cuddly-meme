-- ============================================================================
-- pgbench Read-Only Test
-- ============================================================================
-- Простые SELECT запросы для обычной таблицы
SELECT id, name, balance FROM t_plain WHERE id = 1;
