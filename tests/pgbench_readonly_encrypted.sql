-- ============================================================================
-- pgbench Read-Only Test
-- ============================================================================
-- Простые SELECT запросы для измерения скорости чтения
SELECT id, name, balance FROM t_encrypted WHERE id = 1;
