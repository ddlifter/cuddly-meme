-- ============================================================================
-- pgbench Index Scan Test
-- ============================================================================
-- Поиск по PK-индексу (зашифрованная таблица)
\set idx random(1, 1000)
SELECT id, balance FROM t_encrypted WHERE id = :idx;
